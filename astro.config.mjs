import { defineConfig } from 'astro/config';

// Configuration Astro
// TODO : remplacer par votre vrai nom de domaine une fois réservé
export default defineConfig({
  site: 'https://www.votre-domaine.fr',
});
